# HANDOFF — chosen strategy for this challenge

_Generated by `kit/triage.py` from the measured environment. This is the single
sheet the build agent works from at 5:50._

## Measured environment
- eval time per candidate: **{{EVAL_SECONDS}}s**
- score noise (std): **{{NOISE_STD}}**
- parallel runs allowed: **{{PARALLEL}}**
- repo forkable & submittable: **{{FORKABLE}}**
- affordable generations in time budget: **{{AFFORDABLE_GENS}}**

## Decision
- **Mode {{MODE}}**  (A = in-loop docstring protocol, A+fork = deterministic incumbent seeding)
- **Selector: {{SELECTOR}}**  (greedy | annealed)
- **Strategy bandit layered: {{BANDIT}}**
- **noise margin: {{NOISE_MARGIN}}**
- **also scout offline (Mode B): {{SCOUT_OFFLINE}}**  (never submitted — folds wins into the seed)

### Why
{{RATIONALE}}

## Run it
```sh
{{COMMAND}}
```

## Fallback
If the loop stalls (no accepted improvement for ~3 generations), switch the
selector (greedy→annealed) or, if the strategy bandit is on, force its top
untried hypothesis family. Keep the incumbent; never restart from scratch.
