// Code generated by tenon; DO NOT EDIT.
//
// The Go tool host is its own module so the authored agent module keeps its
// own go.mod and go.sum untouched. It depends on the authored module through a
// local replace and on nothing else: the host is standard library only, so
// preparing a Go tool resolves offline whenever the authored module does.
module {{.HostModule}}

go {{.GoVersion}}

require {{.Module}} v0.0.0

replace {{.Module}} => {{printf "%q" .SourceDir}}
